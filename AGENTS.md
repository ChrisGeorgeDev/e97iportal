<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

## Working with this repo

- Don't spend time verifying UI/route changes with `curl`, spinning up the dev server, or other manual runtime checks after implementing them — the user checks results themselves in the browser. `npm run build` (or type-check) is still worth running since it's cheap and catches real breakage. Skip further "does it look right" checks unless actively debugging a reported bug or explicitly asked to confirm something.

## TODO

- [x] ~~Dashboard breadcrumb shows a title-cased URL slug on post detail pages, not the real post title.~~ Fixed via `components/breadcrumb-title-provider.tsx` — a `BreadcrumbTitleProvider` (wrapping the dashboard layout) plus a `<SetBreadcrumbTitle title="..." />` helper any detail page can render to override the last breadcrumb segment. Wired into both `app/dashboard/news/[slug]/page.tsx` and `app/dashboard/portfolio/[slug]/page.tsx`. Note: the override applies client-side after hydration, so the very first paint briefly shows the slug-derived fallback — expected with this pattern, not a bug.
- [ ] News posts need investor-category targeting. Default is visible to all investor groups; if an admin assigns one or more categories in the post's admin panel, visibility narrows to just those categories. `lib/news.ts`'s `NewsPost` type will need a `categories` field (mirroring the access-category shape already used in `components/side-nav-investor.tsx`), and the listing/detail pages will need to filter by the current investor's categories.
- [ ] News/blog posts can have file attachments. When present, those attachments should also surface in the user's Documents tab (`app/dashboard/documents/page.tsx`, `lib/documents.ts`), listed under a separate "Resources" category so they stay visually distinct from personally-assigned documents.
- [ ] `RegisterInterestDialog` (`components/register-interest-dialog.tsx`) has no backend — "Complete Registration" just flips local state to a thank-you message. Needs a real submission endpoint (likely a Strapi entry tying investor + opportunity + timestamp) once that's available.
- [ ] "Investment Opportunity" is currently just a literal string match on `NewsPost.category` (`lib/news.ts`). Once Strapi is wired up, this should be a proper content-type/category relation instead of a magic string.
- [ ] `InvestmentOpportunities` (`components/investment-opportunities.tsx`) already hides itself when its `opportunities` prop is empty, but `getInvestmentOpportunityPosts()` (`lib/news.ts`) has no concept of active/published status yet — it only checks category. Once Strapi is wired up, that query needs to also filter for an active/published opportunity post, so the module on the news listing page only renders when one genuinely exists and disappears once it's closed/unpublished, not just when the category is unused.
- [ ] Idea for the `RegisterInterestDialog` backend (see above): model the registration as a Strapi relation between the investor and the opportunity post, rather than (or in addition to) a standalone submissions table. That would persist "has this investor already registered" server-side and let the UI reflect prior registrations on return visits, instead of relying on browser cache/local storage — which is lost on a new device, cleared cache, or logging in elsewhere.
- [ ] `lib/portfolio.ts`'s `reportUrl` now comes from Strapi's `portfolio-report.report` media relation instead of a hardcoded path — but no actual report files have been uploaded to Strapi yet, so this is unverified end-to-end. Once investors' self-contained HTML reports are uploaded, confirm the real hosted file renders correctly. The iframe in `app/dashboard/portfolio/[slug]/page.tsx` already has `sandbox="allow-scripts"` set; revisit whether that's still the right sandbox policy once real reports are in place (e.g. if a report needs to load external assets, `allow-same-origin` may be needed too, at some cost to isolation).
- [ ] **Build the authenticated proxy download for Portfolio Reports** so the actual storage URL (Azure blob or otherwise) is never exposed to the client at all — not public, not even a SAS-signed link. Full design (two-hop Next.js → Strapi proxy, reusing the existing `is-account-scoped-detail` policy, why a single Strapi route isn't sufficient since an `<iframe src>` can't carry a bearer token) is written up in `docs/portfolio-report-html-embedding.md` under "Planned: authenticated proxy download." Confirmed the currently-used-elsewhere-in-this-workspace Azure provider (`strapi-provider-upload-azure-storage`, checked both the installed 3.4.0 and latest 3.5.0) cannot generate per-file SAS read URLs — its returned file URL is always bare/unsigned — so this proxy is the actual path to "blob URL protected from direct access," not a provider config option. Worth building before or during the Azure migration, but doesn't require it (works the same against local disk today).
- [x] ~~Clerk auth is wired up... When investor accounts are provisioned, they should also create/sync a corresponding Strapi user/investor record... there's no such sync yet.~~ Done — Strapi now owns this end-to-end: an admin creates an `Invitation` record in Strapi's content-manager (email + Account + role), a lifecycle fires a real Clerk invitation, and a Strapi-hosted webhook (`/api/clerk-webhook`, svix-verified) links the accepted user to their Account on Clerk's `user.created` event. See `/Users/chrisgeorge/.claude/plans/iridescent-painting-goblet.md` for the full design. Remaining setup work before this is live, tracked below.
- [ ] **Strapi admin: enable read permissions for the Authenticated role.** Settings → Users & Permissions → Roles → Authenticated must have `find`/`findOne` checked for Document, PortfolioReport, Property, Investment, and Project, or every request 403s regardless of a valid Clerk token. Leave `create`/`update`/`delete` unchecked (admin-only). Confirmed live during implementation: with zero permissions set, requests correctly 403 rather than 401 — expected Strapi behavior, not a bug.
- [ ] **Fill in real secrets/env vars** for the Clerk↔Strapi integration — `e97-api/.env`: `CLERK_SECRET_KEY`, `CLERK_JWT_KEY` (optional, networkless verification), `CLERK_AUTHORIZED_PARTIES`, `CLERK_WEBHOOK_SIGNING_SECRET`, `INVESTOR_PORTAL_URL`. `e97iportal/.env.local`: `STRAPI_URL` (currently `http://localhost:1337`, needs the real deployed URL outside local dev).
- [ ] **Register the Clerk webhook endpoint** in the Clerk Dashboard pointing at Strapi's own public URL (`https://<strapi-host>/api/clerk-webhook`) for the `user.created` event, and copy its signing secret into `CLERK_WEBHOOK_SIGNING_SECRET`. Also confirm at deploy time whether this custom route actually resolves under `/api/` as expected (flagged as unverified in the design doc — untested against a live Clerk webhook delivery).
- [ ] **Seed real data.** All new Strapi content-types (Account, Project, Investment, Document, PortfolioReport, Property, Invitation) are empty — nothing has been entered through the admin panel yet. This includes uploading the actual self-contained HTML portfolio reports as Strapi media (ties into the `reportUrl` TODO below, which now points at Strapi media but has nothing to point to).
- [ ] **End-to-end test the invite flow** with a real Clerk test account: create an Invitation in Strapi admin → confirm the Clerk invitation is created (Clerk Dashboard) → accept it as a new user → confirm the webhook creates/links the Strapi user and flips `invitation_status` to `accepted` → confirm the signed-in investor only sees their own Account's data, not another seeded account's.
- [ ] **`RegisterInterestDialog` and news/investment-opportunity content-types are still not modeled in Strapi** (see the two TODOs below) — this was explicitly out of scope for the Clerk↔Strapi account-scoping work, not an oversight.
- [x] ~~The custom sign-in form only handles the plain password flow, doesn't check `needs_client_trust`.~~ Fixed — first-ever sign-in from a new device without MFA hit `needs_client_trust` (Clerk's Device Trust feature) and silently stalled (password fields cleared via React 19's form-action reset, no redirect). Added an email-code verification step (`signIn.mfa.sendEmailCode()` / `verifyEmailCode()`) that renders in place of the password form when `signIn.status === "needs_client_trust"`.
- [ ] Real MFA (`needs_second_factor` — TOTP, backup codes) is still unhandled in `app/sign-in/[[...sign-in]]/page.tsx`. Not an issue today since no user has MFA enabled, but add TOTP/backup-code verification UI before enabling MFA on the Clerk instance, or this form will stall the same way `needs_client_trust` did.
